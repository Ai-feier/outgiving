# outgiving

内容生产系统：一个人（验收者）与一组 agent 为四个平台生产文本与视频内容，产出全部是 md 文件，md 是事实源。本文件是已敲定的领域词汇表，不是设计文档：设计看 `ai-video/DESIGN.md`，工作规则看 `AGENTS.md`。

## Language

### 生产链（ai-video）

**链**：
单元构成的只增不改的 DAG，每个节点是一份人能验收的契约。只增、不覆盖、不回改历史。
_Avoid_: pipeline、流水线（暗示线性；链是图）

**层**：
链上的契约块：恰好 1 个 goal + n 个中间单元（可为 0）+ 恰好 1 个 end。层的语义是契约，不是显示块。层只增：goal 不变则原层重跑，goal 变则开新层、旧层标 superseded。
_Avoid_: 段、轮、"5 层单元"里的"层"（那是节，不是层）

**单元**：
一次 AI 事务，一份有稳定身份的契约。一份单元 = 一个主张 + 它的思考、交付物、执行与事后交代。
_Avoid_: 任务、步骤、节点（节点是图视图的显示概念）

**节**：
单元内部的 5 个部分：① 主张、② 思考、③ 内容详情、④ 执行、⑤ 结果。
_Avoid_: "5 层单元"（那个"层"不是链的层）、阶段

**主张**：
单元第 ① 节。人话主张：交付什么 + 关键参数，人可直接改。
_Avoid_: 简层（旧文档的节显示名，非规范术语）

**goal**：
层始单元（恰好 1 个）。它的 ① 是整层的验收标准（逐条可检查），③ 是本层单元的计划。
_Avoid_: 任务、需求单

**end**：
层末单元（恰好 1 个）。逐条拿证据回答 goal ①（验收表）并产出给下一层的交接；任一 claim 不通过则层失败、不交接。
_Avoid_: 总结、收尾

**Run**：
一个运行单元的一次执行尝试。重跑 = 同一单元上新的 Run，不是新单元。
_Avoid_: 重试、轮

**运行单元 / 设计单元**：
交付物需要真实生成调用的单元是运行单元，否则是设计单元。两者的 ③/④ 契约不同：运行单元 ③ 是提示词指针 + 关键参数、④ 是执行记录与 Run；设计单元 ③ 是完整设计件、④ 常为「无 + 理由」。

**门**：
需要人放行、下游才能开工的停止点。两种：层放行门（放行下一层 goal 的 ①）与单元门（指定单元的 ③ 产出后停，人过目）。门可提前关闭，但决策必须留记录。
_Avoid_: checkpoint、检查点（检查点是自动校验，门是人的判断）

**grill**：
人对某单元 ②（思考）的追问，agent 下一轮迭代前必读必答。对别的单元产出的意见 = grill 那个单元的 ②。
_Avoid_: 评审、评论（评审是 end 的验收，grill 是过程中的追问）

**交接**：
把交付物交给下一环。两种形态：视频链的交接块（end ⑤ → 下一层 goal 的输入）；文本管线的共享文件交接（outline → style → 证据 → 图，agent 之间）。
_Avoid_: 交付（交付的对象是读者）

**对齐自报**：
设计单元回读其他单元 ③ 后逐项标注 🔴/🟡/✅。未关闭的 🔴 必须 grill 对方 ② 或触发收口门，由人裁决。

### 角色

**人（验收者）**：
唯一决策者。只做四件事：改 ①、grill ②、过门、验收 end。

**操作员（主会话 agent）**：
创建并落地中间单元、写 goal ③ 计划、层被替代时标 superseded、代人落门记录。单元可提议增删，只有操作员落地。

**工人（designer agent）**：
4 个 designer 及 gather-expert、figure-draftsman。按派单填写单元的 ②③④⑤。是可派发的能力，不是固定管线位。
