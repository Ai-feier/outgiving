# 输出规范

输出目标是让读者最快知道：当前状态是什么、为什么、下一步是什么。

- **操作**（CLI、文件、状态推进）：一句话说清做了什么 + 结果，不解释为什么。
- **元输出**（分析、对比、规划、review）：列表 / 表格优先，不写段落；引用用 `file:line`，不复述代码块。
- **内容创作**（正文、标题、钩子）：不受本节约束，遵循写作 skill。

判断标准：删除这一段后读者仍能正确行动 → 删除。
