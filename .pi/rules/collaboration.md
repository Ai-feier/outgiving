# 协作与交接

- 岗位之间只通过共享文件通信，不通过对话历史。
- 下游必须实际读取上游产出后才能开工；交接点格式必须与上游一致。
- 同一量化指标只在一处定义，其余位置引用；出现第二处定义即为缺陷。
- 外部引用必须标注日期与验证状态（已直接验证 / 未独立验证 / 推测）。
- 渠道知识或工具方法发生变更，必须广播给所有消费它的岗位。

交接点与量化指标的清单位于对应 skill 与 [`system/`](../../system/) 的流程文档，本文件只写约束。
